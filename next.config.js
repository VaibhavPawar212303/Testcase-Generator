/** @type {import('next').NextConfig} */
const nextConfig = {
  // Optimization: Next.js by default handles everything we need.
  // Port 3000 is default, but we'll ensure it's used in scripts.
};

export default nextConfig;
